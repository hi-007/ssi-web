// src/layouts/LoginLayoutVerify.jsx

import { Outlet } from "react-router-dom";

const LoginLayoutVerify = () => {
  return (
    <div>
      <Outlet />
    </div>
  );
};

export default LoginLayoutVerify;
