// src/layouts/LoginLayoutIssue.jsx

import { Outlet } from 'react-router-dom';

const LoginLayoutIssue = () => {
    return (
        <div>
            
            <Outlet />
        </div>
    );
};

export default LoginLayoutIssue;
